export class DetayliArama {
  resim: string;
  kategori: number;
  isim: string;

  constructor(resim: string, kategori: number, isim: string) {
    this.resim = resim;
    this.kategori = kategori;
    this.isim = isim;
  }
}
