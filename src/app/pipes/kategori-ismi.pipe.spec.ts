import { KategoriIsmiPipe } from './kategori-ismi.pipe';

describe('KategoriIsmiPipe', () => {
  it('create an instance', () => {
    const pipe = new KategoriIsmiPipe();
    expect(pipe).toBeTruthy();
  });
});
